<%= form_for(therapist) do |f| %>
  <% if therapist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(therapist.errors.count, "error") %> prohibited this therapist from being saved:</h2>

      <ul>
      <% therapist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :therapist_name %>
    <%= f.text_field :therapist_name %>
  </div>

  <div class="field">
    <%= f.label :default_location %>
    <%= f.text_field :default_location %>
  </div>

  <div class="field">
    <%= f.label :default_rate_pence %>
    <%= f.text_field :default_rate_pence %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
